import { PreventCopyPasswordDirective } from './prevent-copy-password.directive';

describe('PreventCopyPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventCopyPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
